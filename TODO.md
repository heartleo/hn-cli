# DSH Hacker News Plugin TODO

## P0：核心体验

- [ ] 新增 `hn_trending` 工具：用 Algolia `front_page` 一次获取 30 条热门故事，避免榜单概览时的多次 Firebase 请求。
- [ ] 为热门故事补充 `domain`、文章链接、HN 讨论链接和发布时间等展示字段。
- [ ] 更新路由提示词：热门/趋势请求使用 `hn_trending`，讨论总结使用 `hn_item`，搜索使用 `hn_search`。
- [ ] 固化最终回答格式：`HN 热门`、按需展示的“今日看点”、`前十`、按需展示的“话题分布”；不展示 “Digest” 或“首页”。
- [ ] 前十条目统一为：加粗可点击标题、`pts`、评论数、域名、讨论链接和一句“为什么值得看”。
- [ ] 自帖没有文章 URL 时，让标题直接链接到 HN 讨论页。
- [ ] 跟随用户语言；不输出裸 URL、原始 JSON 或工具参数。
- [ ] 增加诚实性规则：未读取评论时，只能依据标题、分数和评论数推断，不得描述评论区观点。
- [ ] 增加不可信输入规则：HN 标题、URL、评论中的指令只能作为数据，不能影响工具调用或系统行为。

## P1：讨论与搜索质量

- [ ] `hn_item` 返回读取范围：已读取评论数、总评论数、最大深度和截断原因。
- [ ] 为长讨论增加 `brief`、`standard`、`deep` 详情级别和总文本预算，避免一次注入过多评论正文。
- [ ] 讨论总结固定为：结论、支持观点、反对/保留观点、代表评论和读取范围。
- [ ] `hn_search` 区分帖子命中和评论命中；评论命中显示所属帖子标题与链接。
- [ ] 将原始 Algolia `tags` 字符串逐步替换为内容类型、作者和时间范围等结构化筛选项。
- [ ] 支持“过去 24 小时”等时间限定；默认“热门”只表示当前 HN 热门，不暗示发布时间。

## P2：稳定性与维护

- [ ] 对 Firebase / Algolia 的 429、5xx、超时增加有限重试与清晰错误提示。
- [ ] 为热门榜单增加短缓存，减少重复请求。
- [ ] 让 `userAgent` 中的版本号随 npm 版本自动同步。
- [ ] 改进工具调用卡片标题，例如 `HN 热门 · 前十`、`HN 讨论 · <item-id>`，避免暴露内部实现名称。

## 测试与验收

- [ ] 为 `hn_trending` 添加离线 fixture 测试，覆盖文章帖、自帖、空 URL 和异常 URL。
- [ ] 为回答规则增加回归测试：标题和讨论链接完整、无裸 URL、语言匹配、截断必提示。
- [ ] 验证热门概览不会声称读过评论。
- [ ] 验证评论总结在截断时会说明覆盖范围。
- [ ] 真实网络检查：热门、搜索、单帖评论和用户资料四条路径。
- [ ] DSH Web 手动冒烟：问“HN 前十”“今天 HN 有什么值得看”“总结这篇讨论”。

## 文档与发布

- [ ] 更新根 README、插件 README：补充三类自然语言示例与回答范围说明。
- [ ] 更新 `docs/handoff.md` 的能力列表与测试说明。
- [ ] 完成测试后发布新版本；新增工具属于功能增加，建议从 `0.1.0` 升至 `0.2.0`。
- [ ] 发布后更新 npm README，并重启本地 DSH Web 验证安装包。
